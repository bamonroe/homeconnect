1) What else can we see in the uploaded data? Do we know how fast the car is going? if its in park? is the turn signal on? Can we enumerate everything we know?

2) It would be nice to be able to speed/slow down video playback

3) I'd like the audio split out of the file that has it - leave it in the original file, but also make a separate new file.
  + If possible, I'd like to have the audio play jointly with the other videos
    that have no audio, but preferably not by splicing into those video files

3) In each drive we should have a "Manage Data" button. 
  + This should allow data to be downloaded via the browser (with selection - so a radio button list to select which items to download - they should be zipped (no compression) before being downloaded).
  + There should be a local delete option - to delete data off the server, again with selection
  + There should be a device delete option - to delete off the comma device
  + There should be "Queue resync" option to re-upload the data from comma to the server the next time its connected

4) In the general settings, we should have a selection for "Upload Defaults" to select which data is uploaded from the comma. 

